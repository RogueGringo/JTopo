# ⚠️ This bundled copy is deprecated

The **canonical home of the `driftwave` plugin** is now its own repository:

### 👉 https://github.com/RogueGringo/driftwave

The `jtopo-plugins` marketplace entry has been repointed to install from there,
so `driftwave@jtopo-plugins` now resolves to the canonical, maintained plugin —
**not** the files in this directory.

The files in `plugins/driftwave/` are a **stale snapshot** (e.g. older license,
manifest, and copy) kept only for git history. Do not treat them as current.
All fixes and new work happen in the standalone repo above.
